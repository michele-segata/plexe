package org.car2x.plexe.protocols;

import org.car2x.plexe.protocols.BBaseProtocol;

simple PlatoformProtocol extends BBaseProtocol
{
    parameters:
        @display("i=block/network2");
        @class(plexe::PlatoformProtocol);
}